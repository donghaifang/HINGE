# Contributing

Thanks for helping improve HINGE.

Before opening a pull request:

1. Keep large datasets, checkpoints, and generated results out of Git.
2. Run `python -m py_compile *.py` from the repository root.
3. Update `README.md` when changing data layout, training flags, or installation steps.
4. Include a short description of the experiment or behavior you verified.

